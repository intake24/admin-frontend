.property
    h3 #{ gettext("Definition") }
    include basic-fields
    .form-group
        label.col-xs-3.control-label(for="food-group-dropdown")
            | #{ gettext("Food group") }
        .col-xs-9
            select.form-control(id='food-group-dropdown'
                                ng-options="fg.id as fg.displayName for fg in foodGroups"
                                ng-model="itemDefinition.main.groupCode")

    include properties-locale

include food-properties-nutrient-codes

include inheritable-properties

include parent-categories

include portion-size

include food-properties-associated-food
